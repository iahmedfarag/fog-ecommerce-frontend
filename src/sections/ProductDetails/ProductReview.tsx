import { styled } from 'styled-components'

export default function ProductReview() {
    return (
        <Wrapper><h1>ProductReview / later</h1></Wrapper>
    )
}

const Wrapper = styled.section`
    padding: 50px 0;
`